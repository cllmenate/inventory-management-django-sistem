services:
  inventory_web:
    build:
      context: .
      target: runtime
    # Usa Uvicorn via Gunicorn para produção
    command: /start.sh
    volumes:
      - static_volume:/inventory-management-django-system/staticfiles
      - media_volume:/inventory-management-django-system/mediafiles
    env_file:
      - .env
    environment:
      - DEBUG=1
      - DJANGO_ENV=development
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS}
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      inventory_db:
        condition: service_healthy
      inventory_redis:
        condition: service_healthy

  inventory_worker:
    build:
      context: .
      target: runtime
    command: celery -A app worker -l info
    healthcheck:
      test: ["CMD-SHELL", "celery -A app inspect ping | grep -q 'OK'"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 20s
    env_file:
      - .env
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      inventory_db:
        condition: service_healthy
      inventory_redis:
        condition: service_healthy

  inventory_flower:
    build:
      context: .
      target: runtime
    command: celery -A app flower --address=0.0.0.0 --port=5555 --broker=${REDIS_URL}
    ports:
      - "5555:5555"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5555/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      inventory_redis:
        condition: service_healthy

  inventory_db:
    image: postgres:17.7-alpine3.23
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  inventory_redis:
    image: redis:8.2.3-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - redis_data_dev:/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  inventory_nginx:
    build: ./nginx
    restart: always
    volumes:
      - static_volume:/inventory-management-django-system/staticfiles
      - media_volume:/inventory-management-django-system/mediafiles
    ports:
      - "80:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      - inventory_web

volumes:
  postgres_data_dev:
  redis_data_dev:
  static_volume:
  media_volume:
